name: Build & FTP Deploy

on:
  push:
    branches: [ "main" ]     # runs after you merge a PR or push to main
  workflow_dispatch: {}       # you can also run it manually

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"
          bundler-cache: true

      - name: Install Jekyll deps
        run: |
          echo 'source "https://rubygems.org"' > Gemfile
          echo 'gem "jekyll", "~> 4.3"' >> Gemfile
          echo 'gem "minima", "~> 2.5"' >> Gemfile
          echo 'gem "jekyll-feed", "~> 0.17"' >> Gemfile
          bundle install --path vendor/bundle

      - name: Build site
        run: bundle exec jekyll build --destination _site

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Upload via FTP (lftp mirror)
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASS }}
          REMOTE_DIR: /home/u819292867/domains/thepolicydispatch.com/public_html/automated-news/
        run: |
          lftp -u "$FTP_USER","$FTP_PASS" "$FTP_HOST" <<'EOF'
          set ftp:ssl-allow no
          set ftp:passive-mode true
          mirror -R --delete --verbose --parallel=4 _site "$REMOTE_DIR"
          bye
          EOF
