# Inoreader JSON stream (your link)
inoreader_json: "https://www.inoreader.com/stream/user/1006265339/tag/Google%20News%20Feeds/view/json?n=1000"

# lookback window, hours
hours_window: 48

# max articles to draft per run
max_articles: 1

# min quality heuristics (title length etc.)
min_title_len: 10

# site defaults
site_url: "https://thepolicydispatch.com/automated-news/"
author: "The Policy Dispatch"

# writing target
target_words: 300

# category/tags (optional)
category: "automated"
tags: ["ai", "news", "brief"]
